# 如文写作系统 5.0 候选版——独立工具最终复审

- 复审日期：2026-08-28（Asia/Taipei）
- 复审对象：冻结候选快照
- 复审方法：运行全部现有单元测试并只读检查实现；未修改插件
- 对照基线：`04-校验记录/独立工具审计.md`

## 最终结论

**PASS。上次独立工具审计及首次复审列出的本地文件与流程阻断项均已关闭。**

本轮核对的原 9 类问题及最终新增 4 类加固全部通过：正式章节写入目录、迁移预览只读、manifest 作用域一致性、packet 活动状态绑定、project-id 格式、账本固定落点、批准事件一次性与原子消费、失败回滚、研究资料职责隔离、表现资源修改后重新取证、handoff 值级 schema 与原文内存重合检查、事件 ID/时间校验、silent floor。

冻结快照现有 35 项测试全部通过，Windows 子进程测试未再出现此前的 UTF-8 解码线程异常。新增测试还实际覆盖 12 个并发进程争用同一批准事件、资源提交失败回滚、handoff 值类型/原文重合、事件身份/时间和 silent floor 同义表达。未发现新的本地发布阻断问题。

## 测试基线

```text
python -m unittest -v test_ruwen_v5.py
Ran 35 tests in 33.572s
OK
```

本轮运行中没有测试失败、跳过或后台解码异常。

## 逐项结果

### 1. 正式章节写入目录限制 — PASS

- `scripts/story_state.py:779-782` 强制正式目标为项目内 Markdown，且必须位于 `chapters/`。
- `test_silent_floor_and_protected_promotion_target` 验证向 `世界/世界总表.md` 提升会被拒绝。
- 正常章节提升、既有章节哈希校验与替换归档测试继续通过。

### 2. 迁移预览不覆盖源文件 — PASS

- `scripts/migrate_v4_1_preview.py:52-64` 拒绝输出位于源项目内，拒绝覆盖既有文件，并使用排他创建模式。
- `test_migration_preview_cannot_write_inside_source` 验证源内输出被拒绝且不产生文件。

### 3. manifest 与登记作用域一致 — PASS

- `scripts/story_state.py:620-665` 将完整可信作用域登记到 active projection。
- `scripts/compile_author_packet.py:249-254` 要求 manifest scope 与登记 scope 完全相等。
- `test_manifest_cannot_self_declare_a_different_scope` 验证自报不同章节会被拒绝。

### 4. packet 必须对应活动状态 — PASS

- `scripts/compile_author_packet.py:401-453` 检查 packets 目录、`state.active_packet` 路径与哈希、登记 meta、schema、manifest 哈希及来源角色。
- `test_rogue_packet_and_tampered_meta_are_rejected` 覆盖 rogue packet 与 meta 篡改。

### 5. project-id 格式与初始化 — PASS

- `scripts/init_project.py:21-22` 限制 project-id 为 3–64 位小写字母、数字、连字符。
- manifest 通过 JSON 解析/赋值/序列化更新，不再进行未转义字符串替换。
- 初始化在临时目录完成后发布；格式拒绝与正常原子初始化测试均通过。

### 6. 账本固定落点与字段约束 — PASS

- `scripts/audit_ledger.py` 以 `--project-root` 固定写入 `治理/审计账本.jsonl`，并严格校验技术字段及固定标量 scope。
- `scripts/problem_ledger.py` 固定写入 `治理/文学问题账本.jsonl`，要求精确字段和长度受限文本。
- 原有任意 `--ledger` 落点已移除。

### 7. 批准事件不可重复、原子消费与失败回滚 — PASS

- `scripts/approval_record.py:22, 40-112` 使用 `治理/审批/已使用/` 下每事件独立标记；先以排他创建模式建立 `.pending`，写入并 `fsync`，提交时以 `os.replace` 原子改名为 `<event-id>.json`。并发创建同一 pending 标记时只有一个调用者成功。
- `scripts/approval_record.py:105-107` 提供预留回滚；统一事件验证同时拒绝 pending 或已提交的 event id。
- candidate：`scripts/story_state.py:696-758` 保存原状态、预留事件、保存新状态、提交消费标记；任一步失败会删除 pending 并恢复原状态。
- memory：`scripts/story_state.py:896-932` 使用相同预留/提交/回滚协议。
- resource activation：`scripts/resource_registry.py:405-423` 预留后原子写注册表，再提交消费标记；失败时回滚标记并恢复注册表原文。
- handoff：执行入口使用统一原子消费函数；其消费本身即执行许可提交点。
- `test_approval_consumption_is_atomic_across_processes` 启动 12 个进程争用同一事件，确认仅 1 个成功且只产生 1 个最终标记；`test_failed_resource_commit_does_not_burn_approval` 验证注册表写失败后批准仍可用。候选与 handoff 一次性测试也继续通过。

### 8. 研究资料不进入写作源 — PASS

- `scripts/resource_registry.py:38-46` 定义资源类型×目录职责矩阵：evidence→`研究/`、character→`人物/`、world→`世界/`、story→`故事/` 等。
- `scripts/resource_registry.py:203-217` 在规范化路径后验证类型与目录一致；`研究/` 文件不能伪装成 world/story/character。
- `scripts/story_state.py:181-206` 继续要求写作输入登记、active、approved、哈希一致，并只允许 character/world/story。
- `test_reconcile_rejects_research_as_story_source` 与 `test_research_directory_cannot_masquerade_as_world_resource` 均通过。

### 9. 表现资源修改后重新取证 — PASS

- `scripts/resource_registry.py:389-390` 拒绝 active expression 直接刷新，要求建立新版本并重新审计、试写、激活。
- 闭合审计、人工复核、试写产物均绑定 resource id、version、规范路径和资源哈希。
- `test_active_expression_cannot_refresh_without_new_evidence` 与 `test_expression_evidence_cannot_be_reused_for_another_resource` 均通过。

## 测试质量复核

此前 Windows 本地运行时，两项 subprocess 测试出现 `UnicodeDecodeError` 但仍显示 `ok`。当前 `test_ruwen_v5.py:616, 694, 707, 720` 为并发及相关 CLI 子进程显式设置 `PYTHONUTF8=1`；本轮完整测试未再出现解码异常。该测试质量问题已关闭。

## 最终新增加固复核

### 10. handoff 值级 schema — PASS

- `scripts/handoff_validator.py:49-134` 为每种 handoff kind 定义精确 payload 字段，并逐项限制文本、文本数组、scope、列表数量和文本长度；`resource_ids` 也要求合法标识数组。
- 顶层与 payload 结构、值类型、枚举和长度必须同时通过，不再只检查键名。

### 11. handoff 原文内存重合检查 — PASS

- `scripts/handoff_validator.py:137-173` 规范化原始表达与 payload 文本，在内存中检查完整包含、反向包含和连续 20 字符长片段重合；明确允许的 `fixed` 字段不参与污染判断。
- CLI 的 `--creation-check` 从标准输入读取当前原始表达，要求非空，并禁止与 `--for-execution` 同时使用（`:270-294`）；没有把原文写入项目文件。
- `test_handoff_values_are_typed_and_raw_wording_is_checked_in_memory` 同时覆盖错误值类型与原文重合。

### 12. 批准事件 ID、时间与哈希 — PASS

- `scripts/approval_record.py:124-150` 要求精确事件字段、规范 event id、可解析 ISO 时间、64 位小写 SHA-256、`approved_by=user`、正确 action，以及与当前目标路径和实际哈希一致。
- `test_approval_event_validation_rejects_empty_identity_and_time` 验证空 ID/空时间被拒绝。

### 13. silent floor — PASS

- `scripts/story_state.py:673-687` 除既有 AI/来源/自检标记外，新增“逐项落实/满足/遵循大纲要求”“研究来源已核对”“全部兑现任务”等同义履约或来源声明模式。
- `record_approved` 在读取候选正文后、批准事件消费前执行 silent floor。
- `test_silent_floor_rejects_synonymous_compliance_language` 与既有 protected promotion 测试均通过。

## 发布判定

在本次复审约定的本地文件与流程范围内：

- 阻断项：0
- 未关闭重要项：0
- 新发现发布阻断：0
- 最终判定：**PASS，可结束工具侧阻断审计。**

本轮已执行同一批准事件的跨进程并发消费测试，并覆盖资源提交失败回滚。本结论仍不扩展到异常掉电恢复或外部系统集成；这些不属于冻结快照的本地工具复审范围。
