# 首版验证报告

日期：2026-08-29  
结果：通过

## 结构验证

- 插件 manifest 验证：通过；
- 技能结构与 frontmatter 验证：通过；
- 运行文件无占位 `TODO`：通过；
- Markdown 差异检查：通过。

技能验证在 Windows 下使用 `PYTHONUTF8=1`，以 UTF-8 读取中文文件。

## 边界验证

- 运行时技能和参考文件不包含《问锋台》的角色、台沿、烧窑等专用内容；
- 明确允许正文已经成立时不做扩写；
- 编辑能力可以处理关系重量，不依赖战斗结构；
- 需要新增上游规则时停止编辑，不编造临时设定；
- 实质修改前保留 Author 原稿；
- 只直接修改批准前候选正文；
- 不加入 Ruwen 工作流；
- 不写正典，不更新人物、世界、故事或记忆。

以上检查全部通过。

## 场景检查

| 场景 | 应有行为 | 结果 |
|---|---|---|
| 战斗结果缺少估值 | 可围绕当前异常深度扩写，不逐招强制说明 | 通过 |
| 关系章节重量不足 | 可使用符合项目声音的叙事承载，不套战斗结构 | 通过 |
| 章节已经充分成立 | 允许放行，不制造编辑工作量 | 通过 |
| 解释需要新增能力规则 | 返回上游，不虚构正典 | 通过 |
| 用户只要求编辑 | 保留原稿并输出候选，不推定批准 | 通过 |
